# Production SPA: call MLB Stats API from the browser (CORS allows *).
# GitHub Actions also sets VITE_PUBLIC_PATH to /your-repo-name/ for project sites.
VITE_API_BASE=https://statsapi.mlb.com/api/v1
