# Production SPA: call MLB Stats API from the browser (CORS allows *).
# GitHub Actions also sets VUE_APP_PUBLIC_PATH to /your-repo-name/ for project sites.
VUE_APP_API_BASE=https://statsapi.mlb.com/api/v1
